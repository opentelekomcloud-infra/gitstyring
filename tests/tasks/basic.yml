---
- hosts: localhost
  gather_facts: false
  roles:
    - gitcontrol
#  tasks:
#  - name: Go to the playbooks folder and execute run.yml
#    command: ansible-playbook run.yml -e github_repos_state=test
#    args:
#      chdir: ../../playbooks
#    register: result
#
#  - debug:
#      msg: Output is {{ result }}
#
#  - name: assert result
#    assert:
#      that:
#        - result is success
#        - result is changed
#        - result.data is defined